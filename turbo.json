{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["build", "^build", "^lint"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["build", "^build", "^typecheck"],
      "outputs": []
    }
  }
}
